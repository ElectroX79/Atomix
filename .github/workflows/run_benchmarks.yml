name: Run benchmarks

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-26.04

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run test script
        run: bash ./scripts/workflow/global_benchmark.sh

